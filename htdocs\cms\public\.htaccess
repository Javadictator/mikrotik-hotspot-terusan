<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /cms/public/

    # Jika request bukan file/folder fisik, lempar ke index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L,QSA]
</IfModule>
